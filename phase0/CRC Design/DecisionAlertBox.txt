Class name: DecisionAlertBox
Parent class:
Subclasses:

Responsibilities:
* Controls the window that appears when adding a new decision
* Adds buttons and a text field inside the window
* Relays the information from the text field to the decision attributes

Collaborators:
*